{
  "name": "mile-compass",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init:local": "wrangler d1 execute mile-compass-db --local --file=./schema.sql && wrangler d1 execute mile-compass-db --local --file=./seed/seed_airports.sql && wrangler d1 execute mile-compass-db --local --file=./seed/seed_flights.sql && wrangler d1 execute mile-compass-db --local --file=./seed/seed_programs.sql && wrangler d1 execute mile-compass-db --local --file=./seed/seed_region_pairs.sql && wrangler d1 execute mile-compass-db --local --file=./seed/seed_distance_bands.sql && wrangler d1 execute mile-compass-db --local --file=./seed/seed_dynamic_ranges.sql && wrangler d1 execute mile-compass-db --local --file=./seed/seed_credit_cards.sql && wrangler d1 execute mile-compass-db --local --file=./seed/seed_season_calendars.sql && wrangler d1 execute mile-compass-db --local --file=./seed/seed_cash_fees.sql && wrangler d1 execute mile-compass-db --local --file=./seed/seed_domestic_bands.sql",
    "db:init:remote": "wrangler d1 execute mile-compass-db --remote --file=./schema.sql && wrangler d1 execute mile-compass-db --remote --file=./seed/seed_airports.sql && wrangler d1 execute mile-compass-db --remote --file=./seed/seed_flights.sql && wrangler d1 execute mile-compass-db --remote --file=./seed/seed_programs.sql && wrangler d1 execute mile-compass-db --remote --file=./seed/seed_region_pairs.sql && wrangler d1 execute mile-compass-db --remote --file=./seed/seed_distance_bands.sql && wrangler d1 execute mile-compass-db --remote --file=./seed/seed_dynamic_ranges.sql && wrangler d1 execute mile-compass-db --remote --file=./seed/seed_credit_cards.sql && wrangler d1 execute mile-compass-db --remote --file=./seed/seed_season_calendars.sql && wrangler d1 execute mile-compass-db --remote --file=./seed/seed_cash_fees.sql && wrangler d1 execute mile-compass-db --remote --file=./seed/seed_domestic_bands.sql"
  },
  "devDependencies": {
    "wrangler": "^4.127.1"
  }
}
